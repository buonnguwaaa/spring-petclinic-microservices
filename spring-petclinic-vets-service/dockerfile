# spring-petclinic-vets-service/Dockerfile
FROM openjdk:17-jdk-slim       
WORKDIR /app
# Copy jar đã build bằng Maven
COPY target/spring-petclinic-vets-service-*.jar app.jar
EXPOSE 8888                      # port admin-server chạy
ENTRYPOINT ["java","-jar","/app.jar"]
